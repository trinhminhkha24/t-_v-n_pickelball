# Pickleball Web App

ASP.NET Core Razor Pages web application for pickleball training exercises and techniques.

## Requirements

- .NET SDK **10** (this project targets `net10.0`).

## Run

From the repo root:

```bash
dotnet watch run --project PickleballWebApp
```

Open:

- https://localhost:7119
- or http://localhost:5215
